/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./app/**/*.{ts,tsx,jsx,js}"], // All files with a `.ts`, `.tsx`, `.js`, or `.jsx` extension will be processed.
  theme: {
    extend: {},
  },
  plugins: [],
}
